_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Gemfile.lock
vendor/
.bundle/
*.gem
.DS_Store

# Internal source material and unreviewed drafts — NEVER commit these.
# The repo is public; raw internal docs must not land in its history.
sources/
drafts/

# Named-entity denylist for the sanitizer. Holds non-public names and
# business facts, so it must never be committed to this public repo.
.claude/redaction-denylist.md

# Voice targets derived from internal documents.
.claude/target-*.json
